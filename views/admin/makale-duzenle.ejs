<!DOCTYPE html>
<html lang="<%= lang %>" dir="<%= lang === 'ar' ? 'rtl' : 'ltr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- __('site.title') %> - Makale Düzenle</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-100">
    <div class="min-h-screen">
        <!-- Admin Header -->
        <header class="bg-gray-900 text-white">
            <div class="container mx-auto px-6 py-4">
                <div class="flex justify-between items-center">
                    <div class="flex items-center">
                        <h1 class="text-xl font-semibold">Admin Paneli</h1>
                    </div>
                    <div class="flex items-center space-x-4">
                        <a href="/admin/makaleler" class="text-gray-300 hover:text-white transition duration-300">
                            Makalelere Dön
                        </a>
                        <span>Hoş geldiniz, <%= user.username %></span>
                        <a href="/admin/logout" class="text-red-400 hover:text-red-300 transition duration-300">Çıkış Yap</a>
                    </div>
                </div>
            </div>
        </header>

        <div class="container mx-auto px-6 py-8">
            <div class="max-w-4xl mx-auto">
                <h2 class="text-2xl font-bold mb-8">Makale Düzenle</h2>

                <form action="/admin/makale/duzenle/<%= article._id %>" method="POST" class="bg-white rounded-lg shadow-lg p-8">
                    <div class="space-y-6">
                        <div>
                            <label for="title" class="block text-gray-700 mb-2">Makale Başlığı</label>
                            <input type="text" id="title" name="title" required
                                value="<%= article.title %>"
                                class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:border-gray-500">
                        </div>

                        <div>
                            <label for="category" class="block text-gray-700 mb-2">Kategori</label>
                            <select id="category" name="category" required
                                class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:border-gray-500">
                                <option value="">Kategori Seçin</option>
                                <option value="Ticaret Hukuku" <%= article.category === 'Ticaret Hukuku' ? 'selected' : '' %>>
                                    Ticaret Hukuku
                                </option>
                                <option value="Aile Hukuku" <%= article.category === 'Aile Hukuku' ? 'selected' : '' %>>
                                    Aile Hukuku
                                </option>
                                <option value="Ceza Hukuku" <%= article.category === 'Ceza Hukuku' ? 'selected' : '' %>>
                                    Ceza Hukuku
                                </option>
                                <option value="Miras Hukuku" <%= article.category === 'Miras Hukuku' ? 'selected' : '' %>>
                                    Miras Hukuku
                                </option>
                                <option value="İş Hukuku" <%= article.category === 'İş Hukuku' ? 'selected' : '' %>>
                                    İş Hukuku
                                </option>
                                <option value="Borçlar Hukuku" <%= article.category === 'Borçlar Hukuku' ? 'selected' : '' %>>
                                    Borçlar Hukuku
                                </option>
                            </select>
                        </div>

                        <div>
                            <label for="content" class="block text-gray-700 mb-2">Makale İçeriği</label>
                            <textarea id="content" name="content" rows="15" required
                                class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:border-gray-500"><%= article.content %></textarea>
                        </div>

                        <div class="flex justify-between items-center">
                            <div class="text-sm text-gray-500">
                                <p>Oluşturulma: <%= new Date(article.createdAt).toLocaleDateString('tr-TR') %></p>
                                <% if (article.updatedAt > article.createdAt) { %>
                                    <p>Son güncelleme: <%= new Date(article.updatedAt).toLocaleDateString('tr-TR') %></p>
                                <% } %>
                                <p>Görüntülenme: <%= article.views %></p>
                            </div>
                            
                            <div class="flex space-x-4">
                                <a href="/admin/makaleler"
                                    class="px-6 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition duration-300">
                                    İptal
                                </a>
                                <button type="submit"
                                    class="px-6 py-2 bg-gray-900 text-white rounded-lg hover:bg-gray-800 transition duration-300">
                                    Değişiklikleri Kaydet
                                </button>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html> 
<!DOCTYPE html>
<html lang="<%= lang %>" dir="<%= lang === 'ar' ? 'rtl' : 'ltr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- __('site.title') %> - <%= article.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar') %>

    <div class="container mx-auto px-6 py-16">
        <div class="max-w-4xl mx-auto">
            <!-- Makale Başlığı ve Meta Bilgiler -->
            <div class="mb-8">
                <h1 class="text-4xl font-bold mb-4"><%= article.title %></h1>
                <div class="flex flex-wrap items-center text-gray-500 text-sm">
                    <span class="bg-gray-200 text-gray-700 px-3 py-1 rounded-full"><%= article.category %></span>
                    <span class="mx-4">•</span>
                    <span>Yazar: <%= article.author.username %></span>
                    <span class="mx-4">•</span>
                    <span><%= new Date(article.createdAt).toLocaleDateString('tr-TR') %></span>
                    <span class="mx-4">•</span>
                    <span><%= article.views %> görüntülenme</span>
                </div>
            </div>

            <!-- Makale İçeriği -->
            <div class="bg-white rounded-lg shadow-lg p-8">
                <div class="prose max-w-none">
                    <%- article.content.replace(/\n/g, '<br>') %>
                </div>
            </div>

            <!-- Son Güncelleme -->
            <% if (article.updatedAt > article.createdAt) { %>
                <div class="mt-4 text-sm text-gray-500">
                    Son güncelleme: <%= new Date(article.updatedAt).toLocaleDateString('tr-TR') %>
                </div>
            <% } %>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html> 